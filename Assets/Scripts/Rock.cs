using UnityEngine; using UnityEngine.InputSystem;  public class Rock: MonoBehaviour {     public int health; // This is for the Yuhui's health.      // Start is called once before the first execution of Update after the MonoBehaviour is created     void Start()     {              }      // Update is called once per frame     void Update()     {         if (health == 0) // If it gets zero it will destroy Yuhui.         {             Destroy(gameObject);         }     }     public void TakeDamage(int damage) // This for taking a damage.     {         health -= damage;     }      public void SpawnClick() // This is for when I click the button it is going to spawn the GameObject, which is Yuhui. However, just ignore this this doesn't work at all. Like         // it is not going to be sosored by the HeroKnight.     {         Vector3 spawnPos = new Vector3(6.07f, 0f, 0f);         if (Mouse.current.leftButton.wasPressedThisFrame == true)         {             GameObject PlsSapwn = Instantiate(gameObject, spawnPos, Quaternion.identity);             health = 30;         }     } }